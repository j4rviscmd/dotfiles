# Build development environment

## WSL2(Windows, Ubuntu, Fish)

### Git

```fish
git config --global user.name "Your Username"
git config --global user.email "your.email@example.com"
git config --global credential.helper cache
```
